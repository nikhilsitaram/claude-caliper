# A10: Integrate re_review_threshold into design-review, plan-review, and implementation-review

**Avoid:** Don't change the re-review logic flow — only replace the hardcoded `5` threshold with a caliper-settings lookup. Don't add new sections or restructure existing ones. Each skill gets a one-liner change.

## Steps

### Step 1: Update `skills/design-review/SKILL.md`

Find the re-review gate:

```markdown
**Re-review gate:** If the reviewer finds more than 5 issues, after all fixes, dispatch a fresh reviewer with the same full scope to confirm clean.
```

Replace with:

```markdown
**Re-review gate:** Read the threshold: `${CLAUDE_PLUGIN_ROOT}/scripts/caliper-settings get re_review_threshold` (default: 5). If the reviewer finds more issues than this threshold, after all fixes, dispatch a fresh reviewer with the same full scope to confirm clean.
```

### Step 2: Update `skills/plan-review/SKILL.md`

Find the re-review gate:

```markdown
**Re-review gate:** If the reviewer finds more than 5 issues, after all fixes, dispatch a fresh reviewer with the same full scope to confirm clean. Under 5 issues, verify fixes and proceed.
```

Replace with:

```markdown
**Re-review gate:** Read the threshold: `${CLAUDE_PLUGIN_ROOT}/scripts/caliper-settings get re_review_threshold` (default: 5). If the reviewer finds more issues than this threshold, after all fixes, dispatch a fresh reviewer with the same full scope to confirm clean. At or under the threshold, verify fixes and proceed.
```

### Step 3: Update `skills/implementation-review/SKILL.md`

Find the Re-Review Gate section:

```markdown
## Re-Review Gate

If the reviewer finds more than 5 fix-needed issues: after all fixes are applied, dispatch a fresh reviewer subagent with the same full review scope. This catches reviewer hallucination from compounding and new issues introduced by bulk fixes.

Under 5 issues, the orchestrator verifies fixes and proceeds without re-review.
```

Replace with:

```markdown
## Re-Review Gate

Read the threshold: `${CLAUDE_PLUGIN_ROOT}/scripts/caliper-settings get re_review_threshold` (default: 5). If the reviewer finds more issues than this threshold: after all fixes are applied, dispatch a fresh reviewer subagent with the same full review scope. This catches reviewer hallucination from compounding and new issues introduced by bulk fixes.

At or under the threshold, the orchestrator verifies fixes and proceeds without re-review.
```

### Step 4: Verify

```bash
grep -q 'caliper-settings get re_review_threshold' skills/design-review/SKILL.md
grep -q 'caliper-settings get re_review_threshold' skills/plan-review/SKILL.md
grep -q 'caliper-settings get re_review_threshold' skills/implementation-review/SKILL.md
```

Commit: `feat: integrate re_review_threshold into review skills`
