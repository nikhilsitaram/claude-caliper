# A6: Update orchestrate SKILL.md with hook documentation

**Avoid:** Don't write a long section. The SKILL.md has a 1,000-word hard cap and is already substantial. The new section should be under 100 words -- just enough to explain the three-layer permission model so the orchestrator knows what's happening. Put it before the "Key Constraints" table as a new "## Permission Model" section.

**Avoid:** Don't duplicate content from the phase dispatcher prompt. The orchestrator doesn't need to know the learning loop details -- that's the dispatcher's job. The orchestrator only needs to know: subagents use auto mode, the hook handles common commands, and the dispatcher asks about new commands.

## Steps

### Step 1: Read current SKILL.md

Read `skills/orchestrate/SKILL.md` to find the right insertion point. Look for the "## Key Constraints" section.

### Step 2: Add Permission Model section

Insert a new section before "## Key Constraints":

```markdown
## Permission Model

Subagents run in `auto` mode -- Claude evaluates each permission request with built-in prompt injection safeguards. A PreToolUse hook (`hooks/pretooluse-safe-commands.sh`) intercepts Bash commands and instantly approves those matching `hooks/safe-commands.txt` prefixes, avoiding per-command AI evaluation overhead for common dev tools. Commands not in the safe list fall through to auto mode evaluation. The phase dispatcher surfaces non-safe commands after each task so the user can grow the safe list.
```

### Step 3: Verify word count and structure

Confirm the new section is under 100 words and the SKILL.md still reads coherently. Check total SKILL.md word count stays under 1,000. Commit:

```text
feat: add permission model section to orchestrate SKILL.md
```

## Handoff from A5

`skills/orchestrate/phase-dispatcher-prompt.md` now has step 9 "Safe commands learning loop" inserted between criteria check (step 8) and cross-phase handoffs (step 10). The prompt explains the full flow: read log, deduplicate, ask via AskUserQuestion, append approved commands to `hooks/safe-commands.txt`, truncate log.
