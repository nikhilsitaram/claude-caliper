# A3: Register PreToolUse hook in hooks.json

**Avoid:** Don't overwrite the existing hooks.json content. The file already has `PostToolUse` and `PermissionRequest` entries (from the accept-edits-hooks feature). Add `PreToolUse` alongside them. Read the file first to confirm the current structure.

## Steps

### Step 1: Read current hooks.json

Read `hooks/hooks.json` to see the existing structure. It should have `PostToolUse` and `PermissionRequest` keys.

### Step 2: Add PreToolUse entry

Add a `PreToolUse` key to hooks.json with a matcher for `Bash` tool and the command pointing to the new hook script:

```json
{
  "PreToolUse": [{
    "matcher": "Bash",
    "hooks": [{
      "type": "command",
      "command": "${CLAUDE_PLUGIN_ROOT}/hooks/pretooluse-safe-commands.sh"
    }]
  }],
  "PostToolUse": [{
    ...existing...
  }],
  "PermissionRequest": [{
    ...existing...
  }]
}
```

The `${CLAUDE_PLUGIN_ROOT}` variable is resolved by Claude Code at runtime to the plugin's installed directory. This matches the pattern used by the existing hook entries.

### Step 3: Verify structure and tests

```bash
jq -e '.PreToolUse[0].matcher == "Bash"' hooks/hooks.json
jq -e '.PostToolUse' hooks/hooks.json
jq -e '.PermissionRequest' hooks/hooks.json
bash tests/hooks/test_safe_commands.sh
```

All existing hook tests (`test_post_tool_use.sh`, `test_permission_request.sh`) should still pass. Commit:

```text
feat: register PreToolUse safe commands hook in hooks.json
```

## Handoff from A2

Files created:
- `hooks/safe-commands.txt` — 39 dev command prefixes, one per line
- `hooks/pretooluse-safe-commands.sh` — PreToolUse hook, reads JSON from stdin, returns `{"permissionDecision":"allow"}` when all commands match safe list, logs non-matches to `$CLAUDE_SAFE_CMDS_LOG` (default: `$TMPDIR/claude-safe-cmds-nonmatch.log`)

All 15 tests in `tests/hooks/test_safe_commands.sh` pass.

The hooks.json structure to match: add "PreToolUse" alongside existing "PostToolUse" and "PermissionRequest" keys, using the same structure as existing entries.
