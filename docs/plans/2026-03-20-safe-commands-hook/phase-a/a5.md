# A5: Add learning loop to phase dispatcher prompt

**Avoid:** Don't add the learning loop logic as a separate section at the bottom of the prompt. It belongs inside the existing "Your Process" numbered list, inserted between the existing step 4 (update plan doc) and step 5 (handoff notes). This way the dispatcher encounters it at the right point in its per-task flow.

**Avoid:** Don't make the AskUserQuestion call complex. The dispatcher should present a simple list of non-matching commands and let the user select which to add. No metadata, no structured format -- just a clear question with the command list.

## Steps

### Step 1: Read current phase-dispatcher-prompt.md

Read `skills/orchestrate/phase-dispatcher-prompt.md` to locate the "Your Process" section. The current numbered steps are:

1. Dispatch implementer subagent
2. After implementer returns: dispatch task reviewer
3. Re-Review Gate
4. Update plan doc
5. After completing a task: handoff notes

### Step 2: Add learning loop step after step 4

Insert a new step 5 between the current step 4 (update plan doc) and step 5 (handoff notes). The current step 5 becomes step 6. The new step:

```text
    5. Safe commands learning loop:
       - Read `$TMPDIR/claude-safe-cmds-nonmatch.log` (may not exist if all commands were safe)
       - If the file exists and is non-empty:
         a. Read and deduplicate the command names (one per line in the log)
         b. Ask the user via AskUserQuestion with a multiSelect-style prompt.
            Present the deduplicated command list numbered, and ask the user to
            select which to add. Example question format:
            "These commands were not in the safe list and were evaluated by auto
            mode during this task. Enter the numbers of any to permanently add
            to the safe list (comma-separated), or 'none' to skip:
            1. brew
            2. cargo
            3. rustc"
         c. For each command the user approves, append it to
            `hooks/safe-commands.txt` (one per line, sorted insertion)
         d. Truncate the log: `> $TMPDIR/claude-safe-cmds-nonmatch.log`
       - If the file doesn't exist or is empty, skip silently
```

### Step 3: Verify the prompt reads correctly

Read the full file and confirm:
- The new step 5 is between plan doc update (step 4) and handoff notes (now step 6)
- The numbering is correct (1-6)
- The instructions are specific enough for a fresh dispatcher subagent to execute without asking questions

Commit:

```text
feat: add safe commands learning loop to phase dispatcher prompt
```
